export const UNTITLED = 'Untitled';

export const SEP = '/';

export const DIRECTORY_ID = {
	root: 'root',
	main: 'main',
	trash: 'trash',
	favorites: 'favorites',
};

export const PATHS = {
	root: SEP,
	main: SEP + DIRECTORY_ID.main,
	trash: SEP + DIRECTORY_ID.trash,
	favorites: SEP + DIRECTORY_ID.favorites,
};
